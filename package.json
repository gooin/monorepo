{
    "private": true,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "prepare": "lerna bootstrap && lerna run build"
    },
    "workspaces": [
        "packages/*"
    ]
}
